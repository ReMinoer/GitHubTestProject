name: Change
on:
  workflow_dispatch:
  push:
    branches: ['**']
    tags-ignore: ['**']
  pull_request:
    branches: ['**']
    tags-ignore: ['**']
jobs:
  validate:
    uses: ReMinoer/GitHubTestProject/.github/workflow/dotnet-validate-workflow.yml@master
