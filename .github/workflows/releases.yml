name: Release
on:
  release:
    types: [published]
jobs:
  dotnet-release:
    name: dotnet
    uses: ReMinoer/GitHubTestProject/.github/workflows/dotnet-release.yml@master
    with:
      project_folder: src/GitHubTestProject.Tool
      target_framework: netcoreapp3.1
      zip_prefix: githubtesttool
  dotnet-nuget:
    needs: dotnet-release
    name: dotnet
    uses: ReMinoer/workflows/.github/workflows/dotnet-nuget.yml@master
    with:
      sources_folder: src
    secrets:
      nuget_token: ${{ secrets.NUGET_TOKEN }}